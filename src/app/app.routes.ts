import { Routes } from '@angular/router';
import { DirectiveDemoComponent } from './directive-demo/directive-demo.component';

export const routes: Routes = [
    {path: '', component: DirectiveDemoComponent}
];
