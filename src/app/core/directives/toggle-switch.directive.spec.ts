import { ToggleSwitchDirective } from './toggle-switch.directive';

describe('ToggleSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
